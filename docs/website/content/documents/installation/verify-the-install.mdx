Check these items:

| Check | Expected result |
|---|---|
| Web console opens | Lore UI loads in the browser |
| Health check passes | database and server are reachable |
| Setup is complete | boot memories and model endpoints are saved |
| Memory page opens | domains and memories can be browsed |
| Recall test works | a query returns candidates or a clear notice |
| Agent runtime connects | the agent can receive boot and recall context |
