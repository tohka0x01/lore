# Guidance configuration is layered

Guidance Configuration will be composed from layers rather than stored as a single monolithic prompt. The Lifecycle Server should combine global guidance, runtime-family overrides, and event-specific templates so common Lore behavior stays shared while host-specific constraints remain localized.
