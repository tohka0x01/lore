# Prompt is a normalized lifecycle field

For `prompt.submit`, the user prompt is a first-class Normalized Lifecycle Field rather than part of the Native Input Snapshot. Recall depends on prompt text as contract data, while the snapshot remains a bounded allowlisted view of additional host-specific fields.
