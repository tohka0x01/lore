# Formatter configuration is declarative

The `formatter` Guidance Section will contain declarative configuration only, not executable code or arbitrary templates. Host-Ready Output rendering stays in server code, while configuration may select renderer variants or parameters; this keeps page-edited guidance validateable, safer to roll back, and easier to test.
