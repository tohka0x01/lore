import SetupLandingPage from './SetupLandingPage';

export const metadata = {
  title: 'Setup · Lore',
  description: 'Configure Lore boot, channel, model, and embedding settings',
};

export default function SetupPage() {
  return <SetupLandingPage />;
}
