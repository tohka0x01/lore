{
  "name": "@local/lore",
  "version": "1.0.6",
  "description": "Lore memory plugin for OpenClaw with fixed boot baseline and recall hooks",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
